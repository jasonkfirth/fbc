/*
    FreeBASIC Windows CE runtime
    ----------------------------

    File: math_pow_data.c

    Purpose:

        Select the Windows CE binary64 logarithm coefficient tables.

    Responsibilities:

        * expose shared pow reduction data to the runtime build

    This file intentionally does NOT contain:

        * executable approximation code
        * compiler constant-folding policy
        * coefficient data for other operating systems
*/

#include "softfloat64/math_pow_data.c"

/* end of math_pow_data.c */
